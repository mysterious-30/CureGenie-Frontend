import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
  preload: true,
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Smart Health Assistance Vending Machine",
  description:
    "Your instant, guided first-aid partner powered by intelligent triage.",
  keywords: ["first aid", "health assistance", "vending machine", "medical"],
  authors: [{ name: "Smart Health" }],
  openGraph: {
    title: "Smart Health Assistance Vending Machine",
    description: "Your instant, guided first-aid partner powered by intelligent triage.",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
